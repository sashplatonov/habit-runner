<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- ── File-level checks ──────────────────────────────────────────── -->
    <module name="NewlineAtEndOfFile">
        <property name="lineSeparator" value="lf"/>
    </module>
    <module name="FileTabCharacter"/>
    <module name="LineLength">
        <property name="max" value="150"/>
        <!-- Ignore package/import statements -->
        <property name="ignorePattern" value="^package |^import "/>
    </module>

    <module name="TreeWalker">
        <!-- ── Import hygiene ─────────────────────────────────────────── -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- ── Naming conventions ─────────────────────────────────────── -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="MemberName"/>
        <module name="ConstantName"/>
        <module name="MatchXpath">
            <property name="query" value="//ENUM_CONSTANT_DEF/IDENT[translate(@text, 'ABCDEFGHIJKLMNOPQRSTUVWXYZ_0123456789', '') != '']"/>
            <message key="matchxpath.match" value="Enum constants must use UPPER_SNAKE_CASE."/>
        </module>
        <module name="LocalVariableName"/>
        <module name="ParameterName"/>

        <!-- ── Code structure ─────────────────────────────────────────── -->
        <module name="OneTopLevelClass"/>
        <module name="NeedBraces"/>
        <module name="MethodLength">
            <property name="max" value="60"/>
            <property name="countEmpty" value="false"/>
        </module>
        <module name="ParameterNumber">
            <property name="max" value="10"/>
        </module>

        <!-- ── Security: forbid catching Throwable ────────────────────── -->
        <module name="IllegalCatch">
            <property name="illegalClassNames" value="java.lang.Throwable"/>
        </module>
    </module>
</module>
