<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Console logging only. For shipping logs to Grafana Cloud Loki, run
       Grafana Agent or Promtail on the host and forward container stdout.
  -->

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{ISO8601} %-5level [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
