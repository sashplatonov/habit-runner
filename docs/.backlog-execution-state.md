completed_item: CDE-009 (Monthly quota) — DONE
commit_hash: HEAD of this commit (`git log -1` at completion; CDE-009 code, backlog status, and execution state share one commit)
next_item: CDE-010 (Monthly weeks)
known_blockers: none
verification_summary: CDE-009 finalized to DONE. Unit tests 15/15 passed in HabitForm.test.ts (new monthly quota test: default 3 seeding with metric/rule copy, increments to the 31 clamp, decrements to the 1 bound with disabled states, Flexible timing copy, dashboard 31x-a-month summary, draft retention across panel back-and-forth, submit payload monthly_quota timesPerMonth). Full E2E habit-journey 44/44 across desktop, compact-mobile, mobile, telegram-webview (warm run). ESLint clean; e2e spec held at exactly 500 code lines. E2E pitfall recorded: click loops must stop exactly at a counter bound (5 weekly from 2->7, 28 monthly from 3->31); overshooting clicks hang on the disabled button until the 30s test timeout. Visual-reference protocol at 390x844 vs 04d_schedule_monthly_quota.html: Completions-per-month counter row with minus/value/plus, metrics (3 / month monthly target, Flexible timing no fixed days), How-it-counts rule 'The month is on target after 3 completions.' with 'Progress is measured against the monthly quota rather than calendar-day attendance.' all match; optional weekdays stay optional with truthful restricted/flexible summary, 44px counter controls, content above the mobile footer; overflow checks pass at 390x844, 320x740, 1280x900. Intentional product-only differences: monthly quota remains an inline sub-view of the Schedule panel per the CDE-005 chooser frame, the optional-weekdays grid (mirroring the flexible-day allowance) and the shared Effect card with the future-history notice stay below the chooser, and the reference has no separate consistency notice on this screen.