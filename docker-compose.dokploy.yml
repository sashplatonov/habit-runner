# Dokploy deployment Compose entry point. Dokploy invokes this file directly,
# so include the complete JVM stack before applying the routing-network overlay.

include:
  - path: ./docker-compose.yml

services:
  api:
    networks:
      - backend
      - dokploy-ipv6

networks:
  dokploy-ipv6:
    external: true
