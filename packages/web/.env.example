# Client environment (copy to `packages/web/.env` for `npm run dev:web` or `turbo dev`).
# Keep this file checked in so everyone shaping the UI sees the default values.
VITE_API_BASE_URL=http://localhost:4000
VITE_SYNC_ENABLED=true
VITE_DEFAULT_USER_ID=demo-user
